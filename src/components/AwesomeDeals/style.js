import React from 'react'
import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  area: {
    flexDirection: 'row',
  },
  item: {
    width: "50%",
    paddingHorizontal: 5,
  }
});
